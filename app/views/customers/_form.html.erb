<% if @customer.errors.any? %>
  <%= pluralize(@customer.errors.count, "error") %> prevented the customer entry frm being created
  <% @customer.errors.full_messages.each do |msg| %>
    <ul>
      <li>
        <%= msg %>
      </li>
    </ul>
  <% end %>
<% end %>
<%= form_for @customer do |f| %>
  <strong><%= f.label :name %></strong>
  <br />
  <%= f.text_field :name %>
  <br /><br />
  <strong><%= f.label :email %></strong>
  <br />
  <%= f.text_field :email %>
  <br /><br />
  <%= f.submit %>
<% end %>
<br />
<%= link_to 'Back to listing customers', customers_path %>