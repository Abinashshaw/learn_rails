<h1>Listing available Images</h1>
Click on the name of image to view them
<br /><br />
<%= link_to 'Go to Home Page', home_index_path %>
<br /><br />
<%= link_to 'Add new image', new_image_path %>
<br /><br />
<table>
  <tr>
    <td><strong>Name</strong></td>
    <td><strong>Caption</strong></td>
  </tr>
  <% @images.each do |image| %>
    <tr>
      <td><%= link_to image.name, image_path(image) %></td>
      <td><%= image.caption %></td>
    </tr>
  <% end %>
</table>